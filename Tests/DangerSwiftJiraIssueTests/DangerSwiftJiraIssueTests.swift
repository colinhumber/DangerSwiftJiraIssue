import XCTest
@testable import DangerSwiftJiraIssue

final class DangerSwiftJiraIssueTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DangerSwiftJiraIssue().text, "Hello, World!")
    }
}
